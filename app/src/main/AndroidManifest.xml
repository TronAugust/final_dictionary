<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dictionary">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.dictionary">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.dictionary.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Definition.DefinitionActivityVIET_ANH"></activity>
        <activity android:name=".Definition.DefinitionActivity"></activity>
        <activity android:name=".favourites.favourite"></activity>
        <activity android:name=".favourites.favouriteVnEng"></activity>
            <activity android:name=".eng_vn"></activity>
        <activity android:name=".yourWord.yourWord"></activity>


    </application>

</manifest>